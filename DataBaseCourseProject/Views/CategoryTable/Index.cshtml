@model List<Category>
@{
    ViewData["Title"] = "Categories";
}

<h1 class="text-center">Categories</h1>

<div>
    <div>
        <table style="width:100%" class="text-center">
            <tr>
                <td style="width:33%">Id</td>
                <td style="width:33%">Name</td>
                <td style="width:33%"></td>
            </tr>
            @foreach (var category in Model)
            {
                <tr>
                    <td style="width:33%">@category.Id</td>
                    <td style="width:33%">@category.Name</td>
                    <td style="width:33%"><a class="btn btn-danger" asp-controller="CategoryTable" asp-action="Delete" asp-route-id="@category.Id">Delete</a></td>
                </tr>
            }
        </table>
    </div>
    <div class="mt-4">
        <a class="nav-link text-white text-light btn btn-dark btn-create"
           asp-area=""
           asp-controller="CategoryTable"
           asp-action="Create">Create</a>
    </div>
</div>